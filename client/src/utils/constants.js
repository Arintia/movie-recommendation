/**
 * @returns {Number} ITEMS_PER_PAGE - A number that defines how many entries can exist on the adminList component before triggering the end of page(pagination).
 */
export const ITEMS_PER_PAGE = 10;